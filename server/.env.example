# Copiá este archivo a .env y completá las credenciales reales.
# server/.env está ignorado por git: NUNCA se sube al repo.
PGHOST=72.60.26.136
PGPORT=5434
PGDATABASE=vectordb
PGUSER=n8nvector
PGPASSWORD=tu_password_aca
PGSSL=false
PORT=8787

# Webhook de mensajes de n8n (Basic Auth). Lo usa el proxy /api/chat.
# La credencial vive SOLO acá (server-side); el frontend ya no la lleva.
N8N_WEBHOOK_URL=https://n8n.agustinynatalia.site/webhook/d7ed99fa-1b54-4d0e-abfe-7c69b9960b7e
N8N_WEBHOOK_USER=admin
N8N_WEBHOOK_PASS=tu_password_de_n8n_aca
